export const DEFAULT_DEMO_WEBSITE_URL = "https://muhammadabdullahwali.vercel.app/";
export const DEFAULT_DEMO_PROJECT_ID = "project_demo_abdullah";
export const DEFAULT_DEMO_BUILD_ID = "build_demo_abdullah_1";

// Frontend/API can run on Vercel, but APK build execution must run elsewhere.
export const BUILD_INFO_NOTE =
  "Frontend can run on Vercel, but Android APK builds must run on a separate Android-capable worker (CI, Docker, VPS, or GitHub Actions runner).";
